apiVersion: v1
kind: Pod
metadata:
  name: web
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
          protocol: TCP
  restartPolicy: Always
